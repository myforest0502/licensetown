# Katakana brand SEO v0.2

Purpose: strengthen the exact branded query `ライセンスタウン` without changing product behavior.

Changes are limited to the public site SEO surface. The visible application UI, learning logic, database, Question Bank, selector, Stage E, and Phase11 are unchanged.

Primary site-name signals now use `ライセンスタウン`, while `LicenseTown` remains an alternate/paired brand name in title text and structured data.

Success is not defined as an immediate ranking guarantee. After production deployment, confirm crawl/index refresh and then observe the exact query `ライセンスタウン` in Google/Search Console over time.
